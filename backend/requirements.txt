# NOTE: torch must be installed separately with CPU-only build.
# See Dockerfile for the pip install command with --index-url.
# Do NOT add torch or transformers here — they are installed in Dockerfile.
fastapi==0.115.0
uvicorn[standard]==0.32.0
yt-dlp>=2024.12.0
opencv-python-headless>=4.10.0
Pillow>=10.4.0
google-generativeai>=0.8.0
youtube-transcript-api>=0.6.0
supabase>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
imagehash>=4.3.1
json-repair>=0.30.0
openai>=1.0.0
markdown>=3.7
